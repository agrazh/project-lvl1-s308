{
  "name": "prj1_brain_games",
  "version": "1.0.1",
  "description": "Collection of console mini-games like \"Brain Games\"",
  "bin": {
    "brain-games": "dist/bin/brain-games.js"
  },
  "files": [
    "dist"
  ],
  "keywords": ["study ptoject", "mini games", "console"],
  "devDependencies": {
    "babel-cli": "*",
    "babel-core": "*",
    "babel-preset-env": "*",
    "babel-preset-stage-0": "*",
    "cross-env": "^5.2.0"
  },
  "scripts": {
    "babel-node": "babel-node",
    "build": "cross-env NODE_ENV=production babel src --out-dir dist --source-maps inline",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "agrazh",
  "license": "ISC",
  "dependencies": {
    "readline-sync": "^1.4.9"
  }
}
